- Certificate support?
    - Be able to:
        - use a certificate
        - detect cert changes w/ tofu
- multi-color lines
    - current object model only supports text + color + bold, but not specifying any of this on the basis of characters
        - this would be useful for link lines, which look nice to have the asterick colored differently than the text
- show redirect destination
    - "do you want to follow redirect to (xyz)?"
- saving files
    - footprints mode
        - download all files to disk for sites you access
    - should show a message after download works
    - should make the filename better
        - lynx just yoinks the final part of the path, it seems
- way to show where links go
- text search
- character encoding options
- don't line break on preformatted text... 
- should reset y value when switching between some tabs
    - persistent memory sometimes though. 
        - perhaps on the basis of a page? sort of wonder about that though; like should this be stored in browser and then perhaps
            - returned when queried for to do rendering? prat of a site? probably not that, not totally sure though. 
                - should be able to highlight arbitrary text / jump to a position on command
                    - what would the abstraction look like for that? 
                        - This would likely take as an input a line text and output a pair with start / stop chars for selection
                            - iff delta p_1 - p_0 > 0 then highlight this line. 
